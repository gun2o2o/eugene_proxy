# Edge Cases & Known Issues

eugene_proxy.py / client.py 사용 시 알아둬야 할 엣지 케이스와 대응 방법을 정리합니다.

---

## 1. 버전 처리 (Version Process)

### 버전 패치 팝업

- `ChampionOpenAPIVersionProcess.exe` 실행 시 업데이트 팝업이 뜰 수 있습니다.
- headless 환경에서는 팝업을 수동으로 클릭해야 합니다.
- 자동화 불가능한 경우, 최초 1회 수동 실행 후 eugene_proxy.py 재시작을 권장합니다.

### 버전 처리 타임아웃

- 네트워크 상태나 서버 점검 시 버전 처리가 `version_timeout` 내에 완료되지 않을 수 있습니다.
- 대응: `setting.ini` 의 `version_timeout` 을 늘리거나, 재시도 로직을 외부에서 구현하세요.

### 관리자 권한

- 일부 환경에서 버전 처리가 실패하면 관리자 권한으로 실행해야 합니다.
- CMD 를 관리자 권한으로 열고 실행하세요.

---

## 2. 로그인

### 다중 접속 거부

- 유진 OpenAPI 는 동일 ID 로 동시 접속을 허용하지 않습니다.
- 이미 HTS/MTS 나 다른 프로세스에서 같은 ID 로 로그인 중이면 에러 코드 -9 가 발생합니다.
- 대응: 다른 접속을 종료하거나, 별도 ID 를 사용하세요.

### 인증서 만료/미등록

- 공인인증서 관련 에러 (-20 ~ -26) 는 인증서 갱신이 필요합니다.
- Champion HTS 에서 인증서를 먼저 등록하세요.

### 비밀번호 오류 횟수

- 비밀번호 5회 이상 오류 시 (에러 -2) 지점 방문이 필요합니다.
- 테스트 시 신중하게 입력하세요.

### 모의투자 vs 실거래

- `setting.ini` 의 `mode` 설정과 실제 서버 선택은 별개입니다.
- 실제 접속 모드는 `OpenAPISvrSelect.exe` 로 변경합니다 (Champion OpenAPI 설치 폴더).
- eugene_proxy.py 의 `mode` 설정은 현재 참고용이며, 서버 전환은 별도 수행해야 합니다.

---

## 3. TR 조회

### 초당 전송 횟수 초과

- 에러 코드 -26: 초당 TR 전송 횟수 제한 초과.
- 대응: 조회 간 `time.sleep(0.5)` 이상의 간격을 두세요.
- 잔고/시세 조회가 많은 경우, 배치로 묶어서 순차 실행하세요.

### 연속 조회 (next_key)

- 보유 종목이 많으면 OutRec2 가 한 번에 모두 반환되지 않을 수 있습니다.
- `request_tr` 호출 시 `next_key` 를 전달하여 연속 조회를 구현하세요.
- 현재 eugene_proxy.py 는 연속 조회 키를 자동으로 반환하지 않으므로, 필요 시 OutRec1 의 연속 관련 필드를 outputs 에 포함하세요.

### TR 응답 타임아웃

- 서버 부하 시 TR 응답이 늦을 수 있습니다.
- client.py 의 `timeout` 파라미터를 조절하세요 (기본 10초).
- 타임아웃 발생 시 `pending_tr` 에 매칭되지 않은 항목이 남을 수 있습니다.

### OutRec 필드명 오류

- 존재하지 않는 필드명을 outputs 에 넣으면 빈 문자열이 반환됩니다 (에러가 아님).
- 유진 OpenAPI 문서 또는 `.qry` 파일에서 정확한 필드명을 확인하세요.
- 참고: `OpenAPI_Package/iOS/.../query/*.qry` 파일에 TR별 필드 정의가 있습니다.

---

## 4. 실시간 데이터

### 실시간 등록 건수 초과

- 에러 코드 -40: 실시간 등록 건수 제한 초과.
- 유진 OpenAPI 는 실시간 등록 종목 수에 제한이 있습니다.
- 대응: 불필요한 종목은 `unsubscribe_real` 로 해제하세요.

### block_len <= 29

- 실시간 데이터에서 `block_len` 이 29 이하인 경우 의미없는 데이터이므로 무시합니다.
- eugene_proxy.py 에서 자동 필터링됩니다.

### 종목코드 매칭 (표준코드 vs 단축코드)

- 실시간 이벤트의 `real_key` 가 표준코드로 올 수 있습니다.
- eugene_proxy.py 에서 `GetShCode()` 를 사용하여 자동 매칭을 시도합니다.
- 매칭 실패 시 해당 이벤트는 무시됩니다.

### 장 마감 후

- 장 마감 후에는 실시간 데이터가 수신되지 않습니다.
- `subscribe_real` 은 성공하지만 데이터가 오지 않을 수 있습니다.

---

## 5. TCP 연결

### 클라이언트 재접속

- 클라이언트가 끊어지면 eugene_proxy.py 는 새 클라이언트 접속을 대기합니다.
- 이전 클라이언트의 실시간 등록은 유지됩니다. 불필요하면 재접속 후 `unsubscribe_all` 호출을 권장합니다.

### 동시 클라이언트

- 현재 eugene_proxy.py 는 1개 클라이언트만 지원합니다.
- 2개째 접속 시 이전 클라이언트는 강제 종료됩니다.

### 방화벽

- `localhost` (127.0.0.1) 에서만 접속하므로 외부 방화벽 이슈는 없습니다.
- 단, Windows Firewall 이 Python 프로세스의 소켓 사용을 차단할 수 있습니다.
- 최초 실행 시 방화벽 허용 팝업이 뜨면 "허용" 을 선택하세요.

### 서버 비정상 종료

- eugene_proxy.py 가 비정상 종료되면 (kill, 크래시) COM 이 정리되지 않을 수 있습니다.
- 이 경우 Champion OpenAPI 프로세스가 남아있을 수 있으므로, 작업관리자에서 확인 후 종료하세요.
- 정상 종료: `client.shutdown()` 또는 Ctrl+C 사용.

---

## 6. 주문

### 모의투자 제한

- 모의투자 환경에서는 일부 주문 유형 (IOC, FOK 등) 이 지원되지 않을 수 있습니다.
- 시장가 주문은 `STK_BD_ORD_UPR` = "0", `ORD_BNS_TCD` = "020" 으로 설정하세요.

### 주문 확인

- 주문 요청 후 `ORD_NO` 가 반환되면 접수 성공입니다. (체결 완료 아님)
- 체결 확인은 별도 TR 조회 또는 실시간 체결 이벤트로 확인해야 합니다.

### 해외주식 주문 시간

- 해외주식은 거래소 운영시간에만 주문 가능합니다.
- 미국: 한국시간 23:30 ~ 06:00 (서머타임 적용 시 22:30 ~ 05:00)

---

## 7. 시스템

### 서버 점검

- 유진투자증권 서버 점검 시 (보통 토/일/공휴일) 접속이 불가합니다.
- `OnAgentEventHandler` 이벤트 타입 51 (소켓 단절) 이 발생할 수 있습니다.

### 다중접속 강제 종료

- `OnAgentEventHandler` 이벤트 타입 50: 다른 곳에서 같은 ID 로 로그인하면 현재 세션이 끊깁니다.
- 대응: `agent_event` 이벤트를 모니터링하고, 자동 재접속 로직을 구현하세요.

### 메모리

- 장시간 운행 시 실시간 데이터가 많으면 이벤트 큐가 쌓일 수 있습니다.
- client.py 에서 `recv_event()` 를 주기적으로 소비하여 큐가 무한히 증가하지 않도록 하세요.

---

## 8. 환경

### 32-bit Python 필수 (eugene_proxy.py 만)

- `ChampionCommAgent.ocx` 는 32-bit COM 전용입니다.
- eugene_proxy.py 를 64-bit Python 에서 실행하면 COM 로드가 실패합니다.
- client.py 와 test.py 는 아무 환경에서나 실행 가능합니다 (순수 표준 라이브러리).

### SSL DLL 이슈

- 일부 32-bit conda 환경에서 SSL 관련 DLL 이 누락될 수 있습니다.
- `libssl-1_1.dll`, `libcrypto-1_1.dll` 을 conda env 의 `DLLs/` 폴더에 복사하세요.
- 현재 환경 (`eugene32`) 에는 이미 적용되어 있습니다.

### 작업 디렉토리

- eugene_proxy.py 실행 시 자동으로 `os.chdir(openapi_path)` 를 수행합니다.
- 이는 COM 컨트롤이 OpenAPI 설치 경로에서 실행되어야 하기 때문입니다.
- setting.ini 경로는 스크립트 위치 기준으로 찾으므로 영향받지 않습니다.
